var loggedIn = false;

if (false === loggedIn) {
    console.log('Please Login');
}

/*
* If adding two equals signs it is called coersion, where we can change values across type
*/

// ***Yoda style: If yoda username is, then proceed ***

// Put what you test for before what it is we are testing against 


// Use Yoda format
var username = 'yoda';

if ('yoda' === username) {
    console.log('Happy Days');
}